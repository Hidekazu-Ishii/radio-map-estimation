# PLATEAU GeoParquet → Sionna RT シーン構築設定

# 入力データ
# data/raw/<city_dir>/bldg/dem/tran/wtr.parquet を想定
cities:
  - city_dir: 13101_chiyoda-ku_2023
    mesh_codes:
      - "53394529"
  - city_dir: 13102_chuo-ku_2023
    mesh_codes:
      - "53393692"

# エリアの一辺の長さ [m]（全 city 共通）
# 3次メッシュ 1枚 ≈ 緯度30秒 × 経度45秒 ≈ 1km²
# bbox はこの値の 1/5 のマージンを加えて取得される
area_size_m: 1000.0
